from setuptools import setup, find_packages

setup(
    name="pplogger",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias
    ],
    entry_points={
        # Puntos de entrada del paquete
    },
    # Otra información como autor, descripción, etc.
)
